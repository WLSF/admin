<h1>Edit Registry</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.registry_path(@conn, :update, @registry)) %>

<span><%= link "Back", to: Routes.registry_path(@conn, :index) %></span>
